import React from "react";

const Header = () => {
    return (
        <div className="ui container fixed">
            <h2>Manage Persons</h2>
        </div>
    );
}

export default Header;